<!-- CONTENT: people/new,html -->
<%= page_title title: 'New Contact for' + @resource.name, form_for: @resource %>
  <%= render 'form', resource: @resource, parent: @parent %>
<%= page_footer() %>
  <%= back_button( { path: organization_path( @parent ) }) %>
<%= page_end() %>