<!-- CONTENT: people/edit -->
<%= page_title( { title: @person.first_name + ' ' + @person.last_name, form_for: @person } ) %>
  <%= render 'form', resource: @person %>
<%= page_footer() %>
  <%= back_button({ path: organization_path(  @org_id ), label: 'Back' }) %>
<%= page_end() %>
