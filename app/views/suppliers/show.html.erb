<!-- CONTENT: Organizations/show.html -->
<%= page_title( { title: 'Supplier Profile' } ) %>
      <%= render 'profile', resource: @resource  %>
    <%= section_title( { title: 'Employees' }) %>
      <%= render 'people/table', resources: @resource.people, parent: @resource %>
  <%= section_title title: 'Status Updates' %>
    <%= link_to 'New Update', new_toe_tag_update_path( @resource ), class: 'btn btn-primary' %>
    <%= render partial: 'status_updates/status_update', collection: @resource.supplier_updates.sort_by_date %>
<%= page_footer() %>
  <%= back_button( { label: 'My Profile', path: process_owner_path( @parent ), buttonType: 'primary', class: 'left' }) %>
  <%= edit_button( { path: edit_process_owner_customer_path( @parent, @resource ), buttonType: 'primary', class: 'right' } ) %>
<%= page_end() %>