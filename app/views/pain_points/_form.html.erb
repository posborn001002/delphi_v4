<%= form_with(model: pain_point, local: true) do |form| %>
  <% if pain_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pain_point.errors.count, "error") %> prohibited this pain_point from being saved:</h2>

      <ul>
      <% pain_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Workshop_id %>
    <%= form.text_field :Workshop_id, id: :pain_point_Workshop_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :pain_point_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
